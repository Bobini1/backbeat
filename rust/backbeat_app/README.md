# Backbeat App

The backbeat app is a desktop application that interfaces with the `backbeat_sdk` API.

It provides all the functions you'd expect for managing your content - but everything it does it part of the `backbeat_sdk` API. If you wanted, everything in here could be directly integrated into the a game client!
